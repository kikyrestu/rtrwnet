const fs = require('fs'); const path = require('path'); function walk(d) { fs.readdirSync(d).forEach(f => { let p = path.join(d, f); if (fs.statSync(p).isDirectory()) walk(p); else if (p.endsWith('.tsx') || p.endsWith('.ts')) { let c = fs.readFileSync(p, 'utf8'); if (c.includes('// err logged')) { let updated = c.replace(/\/\/ err logged[ \t]*}/g, '// err logged\n}'); if (c !== updated) { fs.writeFileSync(p, updated); console.log('Fixed syntax in ' + p); } } } }); } walk('D:/PROJECT/RT-RWNET_PAKAAM/frontend/src');
